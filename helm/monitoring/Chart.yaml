apiVersion: v2
name: finops-monitoring
description: Prometheus + Grafana + AlertManager for FinOps
type: application
version: 1.0.0
dependencies:
  - name: kube-prometheus-stack
    version: "57.0.1"
    repository: "https://prometheus-community.github.io/helm-charts"
